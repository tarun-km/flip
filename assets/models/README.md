# assets/models — not yet populated

Planned home for pinned sherpa-onnx wake-word/STT/TTS model files once
`native/Klip.Windows` is built (see
[`02-SYSTEM-ARCHITECTURE.md`](../../0.%20documentation/02-SYSTEM-ARCHITECTURE.md)
section 5). The current baseline uses the browser's Web Speech API instead
(`apps/desktop/src/renderer/src/lib/voice.ts`) and ships no model binaries.
