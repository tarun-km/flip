# tests/fixtures — not yet populated

Planned home for safe dummy files, synthetic UI targets and fault-injection
fixtures used by the [`03-SRS.md`](../../0.%20documentation/03-SRS.md)
acceptance suite. No automated test suite exists yet in this baseline —
verification so far has been manual (`npm run dev`, `npm run sdk-cli`).
